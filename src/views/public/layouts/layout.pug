doctype html
html(lang="en")
    head
        meta(charset="utf-8")

        meta(
            name="viewport"
            content="width=device-width, initial-scale=1"
        )

        meta(
            name="description"
            content="NDUKA provides discreet marriage intelligence and verification services to help you make informed relationship decisions."
        )

        title= title || 'NDUKA'

        link(
            rel="stylesheet"
            href="/css/public.css"
        )

    body
        include ../partials/nav.pug

        main
            block content

        include ../partials/footer.pug

        script(
            src="/js/app.js"
            defer
        )